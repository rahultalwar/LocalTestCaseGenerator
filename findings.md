# Findings

## Research & Discoveries
- **Ollama Model Names**: API requests require the exact model tag (e.g., `llama3.2:3b`) if `latest` is not explicitly tagged. `llama3.2` strictly looks for `llama3.2:latest`.

## Constraints
- Local execution.
- Use Ollama.
- Installed Model: `llama3.2:3b`.
